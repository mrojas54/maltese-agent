# Validation Plan
Source spec: [SPEC.md](../../SPEC.md) · Source evaluation: [EVALUATION.md](../../EVALUATION.md) · Date: 2026-07-10

Board↔BUILDPLAN ID map: board `MA-1..MA-22` = BUILDPLAN `MA-01..MA-22`; `MA-23` = MA-SP-1 (spike); `MA-24` = MA-H-1 (human); `MA-25` = MA-00 (fix-it test split, operator-routed contract gap).

`pre-merge-static` rows are run by the Result Validator from PR diff + source. `post-merge-smoke` rows are the operator's checklist (listed verbatim in the final report).

| # | Criterion (ID) | Verification method | Artifact to inspect | Pass condition | runnable_at |
|---|---|---|---|---|---|
| 1 | AC-1 | Read `tests/e2e-guard.test.ts` in the PR diff: a test dirties a file under `falcon-agent/`, invokes the guard, asserts skip/fail message names the dirty state AND the file survives byte-for-byte; confirm CI green | MA-2 PR | Test exists asserting both outcomes; CI green | pre-merge-static |
| 2 | AC-2 | Grep post-PR `tests/e2e.test.ts` for `git checkout`, `git reset`, `git clean`, or any mutation of the developer checkout; confirm isolation uses existing `prepWorktree`/`git_worktree_add` | MA-2 PR | Zero checkout-mutating commands; isolation reuses prepWorktree; no new worktree code | pre-merge-static |
| 3 | AC-3 | Read the unit test that stubs an absent falcon-mcp binary and asserts vitest reports **skipped** (not passed) | MA-2 PR | Test asserts skip status; CI green | pre-merge-static |
| 4 | AC-4 | Same as row 3 for absent cassettes | MA-2 PR | Test asserts skip status; CI green | pre-merge-static |
| 5 | AC-5 | Read `.github/workflows/ci.yml`: typescript job sets `E2E_REQUIRED=1` and runs the full suite (`npm run test:full`); rows 3–4 tests prove fail-when-required | MA-2 PR | Env var set in CI; full suite invoked; fail-mode tested | pre-merge-static |
| 6 | AC-6 | Read `falcon-agent/src/interrogate.rs`: planted-lint-#1 marker sits on the `use std::io::Write;` line; H-8 script asserts `cargo check -p falcon-agent` emits exactly that unused-import warning at that line | MA-3 PR | Marker on the real dead import; script-verified; CI green | pre-merge-static |
| 7 | AC-7 | Read `falcon-agent/tests/integration.rs` `#[ignore]` reason: names the intentional workshop smoking-gun, points at README, word "flaky" absent; grep gate present in `scripts/debt-gates.sh` | MA-3 PR | Reason correct; "flaky" absent; H-9 gate appended | pre-merge-static |
| 8 | AC-8 | Read `falcon-agent/src/prompt.rs` module doc: poisoned third example flagged as the intentional plant; grep gate in `scripts/debt-gates.sh` | MA-3 PR | In-file flag present; H-9 gate appended | pre-merge-static |
| 9 | AC-9 | Read `scripts/check-planted-defects.sh`: encodes the three README clippy invocations (`-W clippy::all`; `+ -W clippy::unwrap_used`; `+ -W clippy::nursery`) with expected warning sets; `main.rs:16` `default_constructed_unit_structs` is fixed (canonical set = exactly 3) | MA-1 + MA-3 PRs | Three invocations match README:36-38; 4th lint gone; CI green | pre-merge-static |
| 10 | AC-10 | Read `falcon-mcp/src/tools/subprocess.rs` + per-family integration tests using a hanging fixture with a low configured timeout: child killed, structured timeout error returned | MA-9 PR | Every child-spawning tool wrapped; hanging-fixture tests per family; CI green | pre-merge-static |
| 11 | AC-11 | Read `falcon-mcp/src/tools/limits.rs`: named constants (CARGO_TIMEOUT=900s, GIT_TIMEOUT=60s, SEARCH_TIMEOUT=30s), env override `FALCON_MCP_<NAME>_MS`; tools exposing `timeout_ms` honor per-call override | MA-9 PR | Constants in one module; override honored and tested | pre-merge-static |
| 12 | AC-12 | `rg -c 'z\.any\(\)|as any' falcon-detective/src/handlers/` = 0; `proposePoisonFix` validates via `Issue`/`PoisonReport`; malformed-input unit test asserts a validation error | MA-10 PR | Zero matches; real schemas; malformed-input test present | pre-merge-static |
| 13 | AC-13 | `rg '__definition' falcon-detective/src/` — matches only inside one documented adapter module, or none | MA-11 PR | Private-surface references confined or eliminated | pre-merge-static |
| 14 | AC-14 | Read spike verdict comment on MA-23, then MA-11 PR: Verdict A → AC-13 alone suffices; Verdict B → exact `@barnum/barnum` pin (no caret) AND canary unit test asserting the internal shape, both present | MA-23 comment + MA-11 PR | Verdict-conditional requirements satisfied | pre-merge-static |
| 15 | AC-15 | Read `lib/mcp.ts` + `toolSchemas.ts`: every `FalconMcpClient.call` result zod-validated before typed use; mocked-malformed-response test produces structured error naming tool + mismatch | MA-10 PR | Schema-required call signature; per-tool-result tests | pre-merge-static |
| 16 | AC-16 | Biome config present; `npm run lint` defined and green; CI runs it | MA-5 PR | Config + script + CI step + green | pre-merge-static |
| 17 | AC-17 | Read `ci.yml`: `cargo audit` and `npm audit --audit-level=high` steps; any allowlisted advisories carry an in-repo comment | MA-6 PR | Both steps present; allowlist commented | pre-merge-static |
| 18 | AC-18 | Read `.github/dependabot.yml`: `cargo`, `npm`, `github-actions` ecosystems all covered | MA-6 PR | Three ecosystems configured | pre-merge-static |
| 19 | AC-19 | Read test files: `verify` (Clean/Broken/Stuck), commit handlers (`commitOne`/`commitAll`/`revertOne`/`escalate`, success + git-failure, no silent catch), `finalSweep` (Clean/Dirty), `processIssues` (happy/retry-then-success/revert-on-persistent-failure) — all with mocked MCP + mocked/cassette Gemini | MA-13 PR | Every named branch covered; CI green | pre-merge-static |
| 20 | AC-20 | Smoke tests exist for `cli.ts` (arg parsing) and `workflows/tidy-and-debug.ts` (pipeline composes) | MA-13 PR | Both smoke tests present; CI green | pre-merge-static |
| 21 | AC-21 | Read `falcon-mcp/src/tools/exec.rs`: allowlist resolved to absolute paths at startup; impostor-PATH test proves substitution fails; dead entries `ripgrep`/`sg` dropped; doc strings updated (server.rs:294, exec.rs:6) | MA-14 PR | Absolute-path exec; impostor test; dead entries gone | pre-merge-static |
| 22 | AC-22 | `src/lib/models.ts` exists; `rg 'gemini-' falcon-detective/src --glob '!lib/models.ts'` = 0; grep gate appended to `debt-gates.sh` | MA-8 PR | Single constants module; zero stray literals; gate present | pre-merge-static |
| 23 | AC-23 | `rust-toolchain.toml` pins 1.92; CI derives toolchain from the file; `debt-gates.sh` checks Dockerfile base image matches the pinned minor | MA-4 PR | Pin + CI derivation + Dockerfile check | pre-merge-static |
| 24 | AC-24 | Read `makeProposeFixHandler` factory: three propose* handlers are instances (prompt file + model per kind); parity tests prove behavior unchanged | MA-12 PR | One implementation; parity tests green | pre-merge-static |
| 25 | AC-25 | `fs_search` and `fs_search_ast` share one streaming-subprocess helper; `rg -c 'MAX_STDERR_DISPLAY\s*[:=]' falcon-mcp/src/` = 1 definition; existing search tests green | MA-9 PR | Shared helper; constant defined once; CI green | pre-merge-static |
| 26 | AC-26 | Read `package.json`: vitest ≥3, typescript current, @types/node aligned, Node engine stays 20; H-4/H-5/H-6 green in CI | MA-7 PR | Versions correct; CI green | pre-merge-static |
| 27 | AC-27 | Read `package.json`: `@google/genai` on 2.x; build compiles; gemini unit tests green on mocked client | MA-19 PR | 2.x dependency; CI green | pre-merge-static |
| 28 | AC-28 | **Operator**: `GEMINI_API_KEY=… npm run record`, then `npm run test:full` in cassette mode passes on the new cassettes | Merged main after MA-24 | e2e green on re-recorded cassettes | post-merge-smoke |
| 29 | AC-29 | Read `Cargo.toml` (both crates): axum 0.8, cargo_metadata current; H-2/H-3 green; H-8 (planted-defect guard) green on the same PR | MA-21 PR | Versions current; workspace builds; G-1 intact | pre-merge-static |
| 30 | AC-30 | Read decomposed `processIssues`: named stage functions each ≲40 lines; AC-19 tests unchanged and green | MA-15 PR | Decomposition done; tests stayed green | pre-merge-static |
| 31 | AC-31 | Read `fs_apply_patch` split: validate/parse/splice units, each with unit tests | MA-16 PR | Three units, each tested | pre-merge-static |
| 32 | AC-32 | Read `server.rs` ToolError taxonomy: ≥3 distinguishable kinds with distinct MCP error codes across all 18 former `.map_err(format!)` sites; client-branch test present | MA-17 PR | Distinct codes; branch test; CI green | pre-merge-static |
| 33 | AC-33 | Read `gemini.ts`: backoff between schema-retry attempts; typed/broadened transient detection covering network failures; unit tests simulate both | MA-18 PR | Both behaviors tested | pre-merge-static |
| 34 | AC-34 | `rg -c 'detective:\s*any|as any' falcon-detective/src/workflows/` = 0; AC-20 workflow smoke compiles without casts | MA-15 PR | Zero casts; smoke compiles | pre-merge-static |
| 35 | G-1 (static) | `scripts/check-planted-defects.sh` wired into CI at MA-3 and green on every subsequent PR touching falcon-agent (esp. MA-21) | MA-3 PR + CI runs of later PRs | Guard in CI; no red H-8 run ever merged | pre-merge-static |
| 36 | G-1 (smoke) | Run `scripts/check-planted-defects.sh` on final merged main | Merged main | Exit 0: poisoned example, missing guards, 3 planted lints, ignored smoking-gun all intact and correctly marked | post-merge-smoke |
| 37 | G-2 | Diff-review every falcon-mcp PR (MA-9, MA-14, MA-16, MA-17, MA-21): existing sandbox tests (path escape, allowlist, read-only) unmodified-or-strengthened and green | MA-9/14/16/17/21 PRs | No sandbox test weakened or deleted; all green | pre-merge-static |
| 38 | G-3 | `git log --merges` + PR list on final main: no force-pushes, per-ticket conventional commits, every merge via green CI | Merged main history | History clean per G-3 | post-merge-smoke |
| 39 | F-1 | **Operator** drives the detective pipeline end-to-end in cassette mode after M3 (MA-9..MA-14 merged): readable story, sensible per-issue commits | Merged main at M3 | Operator signs off F-1 | post-merge-smoke |
| 40 | F-2 | **Operator** terminal drive on the upgraded stack after MA-22, immediately after AC-28 | Merged main at M4 | Operator signs off F-2 | post-merge-smoke |
| 41 | Final sweep (MA-22) | Run all harnesses H-1..H-9 on merged main | Merged main | All nine green | post-merge-smoke |
| 42 | Fix-it split (MA-25) | `npm test` runs hermetically (no e2e, no binary, no cassettes) in ≤60s; `npm run test:full` includes e2e | MA-25 PR | Both targets behave as specified | pre-merge-static |
