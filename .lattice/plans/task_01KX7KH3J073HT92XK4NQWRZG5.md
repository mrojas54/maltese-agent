# MA-35: Native fs_search in falcon-mcp, drop the ripgrep PATH dependency

CLOSEOUT CANDIDATE minted at operator request (2026-07-10, out of contract 9f72697). Origin: fs_search shells out to ripgrep on PATH; on GitHub runners (no rg) it threw, a silent catch degraded the triage poison signal to [], the prompt content changed, and every cassette key missed in CI — the root cause of the PR #64 saga. CI now installs rg, but the PATH dependency remains a portability landmine for every new environment. Scope: replace the rg subprocess with a native Rust search (the ripgrep component crates grep-searcher/grep-regex/ignore are the natural fit) BEHIND THE EXACT SAME TOOL OUTPUT SHAPE — fs_search results feed triage prompts which feed cassette keys, so any format drift (paths, line numbers, match text, ordering) breaks replay. Gates: cargo fmt/clippy/test green; full e2e cassette replay with ZERO misses (this IS the output-parity proof); only after that may the CI rg-install step be removed (same PR, separate commit). G-1: falcon-agent plants untouched; the loud console.error diagnostics in triage.ts stay. Size M.
