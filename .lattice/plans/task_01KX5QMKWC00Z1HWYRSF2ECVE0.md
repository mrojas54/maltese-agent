# MA-3: Fix intentional-defect markers and wire guard into CI

BUILDPLAN M1 / WS-2 (BUILDPLAN id MA-03). Fix MARKERS in falcon-agent, never the planted defects (G-1): move planted-lint-1 marker to the real dead import use std::io::Write (interrogate.rs:3); rewrite the ignore reason on bird_themed_inputs_arent_special to name the intentional workshop smoking-gun with README pointer (the word flaky must not appear); flag the poisoned third example in prompt.rs module doc; FIX the undocumented 4th lint default_constructed_unit_structs at main.rs:16 (canonical planted set stays exactly three). Wire the MA-01 check-planted-defects.sh into CI in THIS PR. Append AC-7/AC-8 grep gates to scripts/debt-gates.sh (SPEC R-5). ACs: AC-6..AC-9. Depends: MA-01, MA-04. Serialize: ci.yml. Size S.
